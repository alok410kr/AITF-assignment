CREATE A FILE NAMED ".env" IN THIS FOLDER (frontend)

Copy the content below and paste it into your .env file:

========== COPY BELOW THIS LINE ==========

VITE_APP_API_URL=http://localhost:3001
VITE_APP_ENVIRONMENT=development

========== COPY ABOVE THIS LINE ==========

This file is optional but recommended for local development.
Save the file as ".env" (not .env.txt, just .env)

